<div id="form">
<%= form_for(@bind) do |f| %>
  <% if @bind.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bind.errors.count, "error") %> prohibited this bind from being saved:</h2>

      <ul>
      <% @bind.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'セグメント' %>:
    <%= f.collection_select :segment_id, Segment.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label 'アクション' %>:
    <%= f.collection_select :con_action_id, ConAction.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit "作成" %>
  </div>
<% end %>
</div>
